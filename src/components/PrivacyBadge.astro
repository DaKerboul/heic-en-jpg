---
import Icon from './Icon.astro';
interface Props {
  compact?: boolean;
}
const { compact = false } = Astro.props;
---
<div class={`flex items-start gap-3 rounded-2xl border border-emerald-200/80 bg-emerald-50 shadow-sm dark:border-emerald-900 dark:bg-emerald-950 ${compact ? 'px-3 py-2 text-[13px]' : 'px-4 py-3 text-sm'}`} role="note" aria-label="Garantie de confidentialité">
  <span class="flex h-8 w-8 shrink-0 items-center justify-center rounded-full bg-emerald-500 text-white shadow-sm" aria-hidden="true"><Icon name="lock-simple" class="h-4 w-4" /></span>
  <p class="leading-snug text-emerald-950 dark:text-emerald-100">
    <strong>Vos photos ne quittent jamais votre appareil.</strong>
    <span class="text-emerald-800 dark:text-emerald-300"> Conversion 100&nbsp;% locale (WebAssembly + Canvas) — vérifiable en coupant le Wi-Fi après chargement, ou dans l'onglet réseau : 0 envoi.</span>
  </p>
</div>
